<?xml version="1.0" encoding="UTF-8"?>
<project name="UrnaBrasil" default="war" basedir=".">
    <description>Build standalone para Docker — não depende do NetBeans</description>

    <property name="src.dir"    value="src/java"/>
    <property name="web.dir"    value="web"/>
    <property name="lib.dir"    value="lib"/>
    <property name="build.dir"  value="build-docker"/>
    <property name="classes.dir" value="${build.dir}/classes"/>
    <property name="dist.dir"   value="dist"/>
    <property name="war.file"   value="${dist.dir}/UrnaBrasil.war"/>

    <!-- Tomcat servlet-api e jsp-api vêm da imagem Docker -->
    <property name="tomcat.home" value="/usr/local/tomcat"/>

    <path id="compile.classpath">
        <fileset dir="${lib.dir}" includes="*.jar"/>
        <fileset dir="${tomcat.home}/lib" includes="servlet-api.jar,jsp-api.jar,el-api.jar" erroronmissingdir="false"/>
    </path>

    <target name="clean">
        <delete dir="${build.dir}"/>
        <delete dir="${dist.dir}"/>
    </target>

    <target name="init" depends="clean">
        <mkdir dir="${classes.dir}"/>
        <mkdir dir="${dist.dir}"/>
    </target>

    <target name="compile" depends="init">
        <javac srcdir="${src.dir}"
               destdir="${classes.dir}"
               includeantruntime="false"
               encoding="UTF-8"
               source="11"
               target="11"
               classpathref="compile.classpath"/>
    </target>

    <target name="war" depends="compile">
        <war destfile="${war.file}" webxml="${web.dir}/WEB-INF/web.xml">
            <!-- Inclui tudo de web/ exceto o web.xml (já incluído pelo atributo webxml) -->
            <fileset dir="${web.dir}">
                <exclude name="WEB-INF/web.xml"/>
            </fileset>
            <!-- Classes compiladas -->
            <classes dir="${classes.dir}"/>
            <!-- JARs de dependência (JSF) -->
            <lib dir="${lib.dir}"/>
        </war>
        <echo message="WAR gerado com sucesso em: ${war.file}"/>
    </target>
</project>
